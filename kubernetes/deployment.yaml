apiVersion: apps/v1
kind: Deployment
metadata:
  name: kavak-chatbot-deployment
  labels:
    app: kavak-chatbot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kavak-chatbot
  template:
    metadata:
      labels:
        app: kavak-chatbot
    spec:
      containers:
      - name: kavak-chatbot
        image: ghcr.io/darkkaze/kavak_llm_testinterview:main
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: kavak-chatbot-config
        - secretRef:
            name: kavak-chatbot-secret
        - secretRef:
            name: kavak-twilio-secret
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          # No limits set to allow bursting
